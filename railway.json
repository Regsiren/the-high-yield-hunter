{
  "$schema": "https://railway.app/v2/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn main:app --bind 0.0.0.0:${PORT:-8080}",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}
